1. final 필드
- final 필드는 초기값이 저장되면 이것이 최종적인 값이 되어 프로그램 실행 도중에 수정 할 수 없다는 것이다.
final 타입 필드 = 초기값;
- 초기값 주는 방법
	- 필드 선언시에 주는 방법
	- 생성자를 통한 방법
	
2. 상수
- 불변의 값을 상수라고 한다.
- 상수는 static 이면서 final이여야 한다.
static final 타입 상수 = 초기값;
- 초기값 주는 방법
	- 필드 선언시에 주는 방법
	- static 블록을 통한 방법
- 상수 이름은 모두 대문자로 작성하는 것이 관례적이다.

3. final필드과 상수의 다른점
- 상수는 공용의 변수로써 불변의 값이고, final필드는 각 객체마다 고유한 값이라고 보면 된다. 